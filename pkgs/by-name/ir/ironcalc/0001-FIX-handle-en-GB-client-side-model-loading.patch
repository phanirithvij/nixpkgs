From 472fb03cd12aaa2b45bf0207e6bf1f6fd3df8ad8 Mon Sep 17 00:00:00 2001
From: phanirithvij <phanirithvij2000@gmail.com>
Date: Tue, 28 Apr 2026 14:35:00 +0530
Subject: [PATCH 1/6] FIX: handle en-GB client side model loading

Signed-off-by: phanirithvij <phanirithvij2000@gmail.com>
---
 webapp/app.ironcalc.com/frontend/src/App.tsx  | 22 +++++++++----------
 .../frontend/src/components/storage.ts        |  2 +-
 2 files changed, 11 insertions(+), 13 deletions(-)

diff --git a/webapp/app.ironcalc.com/frontend/src/App.tsx b/webapp/app.ironcalc.com/frontend/src/App.tsx
index c836eb0..266831c 100644
--- a/webapp/app.ironcalc.com/frontend/src/App.tsx
+++ b/webapp/app.ironcalc.com/frontend/src/App.tsx
@@ -19,6 +19,7 @@ import {
   createNewModel,
   deleteModelByUuid,
   deleteSelectedModel,
+  getLanguageFromLocale,
   getShortLocaleCode,
   isStorageEmpty,
   loadDefaultLocaleFromStorage,
@@ -61,14 +62,14 @@ function App() {
       const modelHash = urlParams.get("model");
       const exampleFilename = urlParams.get("example");
       const language = loadDefaultLocaleFromStorage();
-      const localeShort = getShortLocaleCode(language);
+      const languageId = getLanguageFromLocale(language);
       // If there is a model name ?model=modelHash we try to load it
       // if there is not, or the loading failed we load an empty model
       if (modelHash) {
         // Get a remote model
         try {
           const model_bytes = await get_model(modelHash);
-          const importedModel = Model.from_bytes(model_bytes, localeShort);
+          const importedModel = Model.from_bytes(model_bytes, languageId);
           localStorage.removeItem("selected");
           setModel(importedModel);
         } catch (_e) {
@@ -77,7 +78,7 @@ function App() {
       } else if (exampleFilename) {
         try {
           const model_bytes = await get_documentation_model(exampleFilename);
-          const importedModel = Model.from_bytes(model_bytes, localeShort);
+          const importedModel = Model.from_bytes(model_bytes, languageId);
           localStorage.removeItem("selected");
           setModel(importedModel);
         } catch (_e) {
@@ -183,8 +184,8 @@ function App() {
 
             const bytes = new Uint8Array(await blob.arrayBuffer());
             const locale = loadDefaultLocaleFromStorage();
-            const localeShort = getShortLocaleCode(locale);
-            const newModel = Model.from_bytes(bytes, localeShort);
+            const languageId = getLanguageFromLocale(locale);
+            const newModel = Model.from_bytes(bytes, languageId);
             saveModelToStorage(newModel);
 
             setModel(newModel);
@@ -221,11 +222,8 @@ function App() {
               default: {
                 const model_bytes = await get_documentation_model(templateId);
                 const locale = loadDefaultLocaleFromStorage();
-                const localeShort = getShortLocaleCode(locale);
-                const importedModel = Model.from_bytes(
-                  model_bytes,
-                  localeShort,
-                );
+                const languageId = getLanguageFromLocale(locale);
+                const importedModel = Model.from_bytes(model_bytes, languageId);
                 saveModelToStorage(importedModel);
                 setModel(importedModel);
                 break;
@@ -246,8 +244,8 @@ function App() {
           onSelectTemplate={async (fileName) => {
             const model_bytes = await get_documentation_model(fileName);
             const locale = loadDefaultLocaleFromStorage();
-            const localeShort = getShortLocaleCode(locale);
-            const importedModel = Model.from_bytes(model_bytes, localeShort);
+            const languageId = getLanguageFromLocale(locale);
+            const importedModel = Model.from_bytes(model_bytes, languageId);
             saveModelToStorage(importedModel);
             setModel(importedModel);
             setTemplatesDialogOpen(false);
diff --git a/webapp/app.ironcalc.com/frontend/src/components/storage.ts b/webapp/app.ironcalc.com/frontend/src/components/storage.ts
index bea6699..8ac3611 100644
--- a/webapp/app.ironcalc.com/frontend/src/components/storage.ts
+++ b/webapp/app.ironcalc.com/frontend/src/components/storage.ts
@@ -57,7 +57,7 @@ export function getShortLocaleCode(longCode: string): string {
 }
 
 // en-US => en, en-GB => en, es-ES => es, fr-FR => fr, de-DE => de, it-IT => it
-function getLanguageFromLocale(locale: string): string {
+export function getLanguageFromLocale(locale: string): string {
   return locale.split("-")[0];
 }
 
-- 
2.53.0

